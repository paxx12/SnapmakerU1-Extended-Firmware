--- a/etc/init.d/S91unisrv
+++ b/etc/init.d/S91unisrv
@@ -30,6 +30,13 @@ ARGS="-t $(camera_type)"

 case "$1" in
   start)
+	EXTENDED_CFG="/home/lava/printer_data/config/extended/extended2.cfg"
+	CAMERA_INTERNAL=$(/usr/local/bin/extended-config.py get "$EXTENDED_CFG" camera internal paxx12)
+	if [ "$CAMERA_INTERNAL" != "snapmaker" ]; then
+		echo "Internal camera is not set to 'snapmaker', not starting unisrv."
+		exit 0
+	fi
+
 	log "Starting unisrv process..."
 	ulimit -c 102400
 	start-stop-daemon -S -b -x "$PROG" -m -p "$PIDFILE" -- $ARGS
