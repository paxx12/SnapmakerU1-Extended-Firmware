# SPDX-License-Identifier: GPL-3.0-or-later
# SPDX-PackageHomePage: https://github.com/paxx12-snapmaker-u1/SnapmakerU1-Extended-Firmware
# SPDX-FileCopyrightText: Copyright (c) 2025 @paxx12, @horzadome

name: Build

on:
  push:
    branches:
      - develop
    paths-ignore:
      - '*.md'
      - .github/workflows/*.yaml
  pull_request:
  workflow_dispatch:

# Change this one value when building a mod profile. Keep `extended` for the
# normal unmodded firmware; compose mods as `extended-<mod1>-<mod2>`.
# Profile naming rules: docs/mods.md#adding-a-mod
env:
  BUILD_PROFILE: extended

jobs:
  custom-firmware-build:
    runs-on: ubuntu-24.04-arm
    permissions:
      contents: read
    steps:
    - name: Record start time
      run: echo "JOB_START=$(date +%s)" >> $GITHUB_ENV

    - name: Checkout
      uses: actions/checkout@v3
      with:
        fetch-depth: 0
        submodules: recursive

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Build Docker image with cache
      uses: docker/build-push-action@v6
      with:
        context: .github/dev
        load: true
        tags: snapmaker-u1-dev:latest
        cache-from: type=gha
        cache-to: type=gha,mode=max
        build-args: BUILDKIT_INLINE_CACHE=1

    - name: Load values vars.mk
      run: cat vars.mk >> "$GITHUB_ENV"

    - name: Restore Firmwares Cache
      id: cache-firmwares
      uses: actions/cache/restore@v4
      with:
        path: firmware/
        key: firmwares-${{ runner.os }}-${{ runner.arch }}-${{ env.FIRMWARE_VERSION }}

    - name: Restore Kernel Cache
      id: cache-kernel
      uses: actions/cache/restore@v4
      with:
        path: tmp/kernel/
        key: kernels-${{ runner.os }}-${{ runner.arch }}-${{ env.KERNEL_SHA }}

    - name: Restore ccache
      id: cache-ccache
      uses: actions/cache/restore@v4
      with:
        path: tmp/ccache/
        key: ccache-${{ runner.os }}-${{ runner.arch }}-${{ github.head_ref || github.ref_name }}
        restore-keys: |
          ccache-${{ runner.os }}-${{ runner.arch }}-

    - name: Restore downloads cache
      id: cache-downloads
      uses: actions/cache/restore@v4
      with:
        path: |
          tmp/cache/**/.git
          tmp/cache/**/*.zip
          tmp/cache/**/*.gz
          tmp/cache/**/*.xz
        key: downloads-${{ runner.os }}-${{ runner.arch }}-${{ github.head_ref || github.ref_name }}
        restore-keys: |
          downloads-${{ runner.os }}-${{ runner.arch }}-

    - name: Set GIT_VERSION (for branch)
      shell: bash
      if: github.event_name == 'push'
      run: |
        nextVer=$(./scripts/next_version.sh paxx12 test-branch-${{ github.ref_name }})
        echo "GIT_VERSION=${nextVer//v/}" | tee -a $GITHUB_ENV

    - name: Set GIT_VERSION (for Pull Request)
      shell: bash
      if: github.event_name == 'pull_request'
      run: |
        nextVer=$(./scripts/next_version.sh paxx12 test-pr-${{ github.event.pull_request.number }})
        echo "GIT_VERSION=${nextVer//v/}" | tee -a $GITHUB_ENV

    - name: Download firmware
      run: ./dev.sh make firmware

    - name: Build selected firmware profile
      run: ./dev.sh make build PROFILE=${{ env.BUILD_PROFILE }} OUTPUT_FILE=U1_${{ env.BUILD_PROFILE }}_${{ env.GIT_VERSION }}_upgrade.bin

    - uses: actions/upload-artifact@v4
      with:
        name: ${{ env.BUILD_PROFILE }}-build
        path: U1_${{ env.BUILD_PROFILE }}_${{ env.GIT_VERSION }}_upgrade.bin

    - name: Build with Extended Fluidd + AFC firmware (experimental)
      run: ./dev.sh make build PROFILE=extended-afc OUTPUT_FILE=U1_extended-afc_${{ env.GIT_VERSION }}_upgrade.bin

    - uses: actions/upload-artifact@v4
      with:
        name: extended-afc-build
        path: U1_extended-afc_*_upgrade.bin

    - name: Build PR630 ACE firmware (experimental)
      run: ./dev.sh make build PROFILE=extended-tareku99 OUTPUT_FILE=U1_extended-tareku99_${{ env.GIT_VERSION }}_upgrade.bin

    - uses: actions/upload-artifact@v4
      with:
        name: extended-tareku99-build
        path: U1_extended-tareku99_*_upgrade.bin

    - name: Save PR info
      if: github.event_name == 'pull_request'
      run: |
        mkdir -p pr-info
        echo "${{ github.event.pull_request.number }}" > pr-info/number
        echo "${{ github.event.pull_request.head.sha }}" > pr-info/head_sha
        echo "${{ github.sha }}" > pr-info/merge_sha
        echo "${{ github.base_ref }}" > pr-info/base_ref
        echo "${{ env.GIT_VERSION }}" > pr-info/version
        echo "${{ env.JOB_START }}" > pr-info/job_start
        echo "$(date +%s)" > pr-info/job_end
        echo "${{ env.BUILD_PROFILE }}-build" > pr-info/artifacts
        echo extended-afc-build >> pr-info/artifacts
        echo extended-tareku99-build >> pr-info/artifacts

    - name: Upload PR info
      if: github.event_name == 'pull_request'
      uses: actions/upload-artifact@v4
      with:
        name: pr-info
        path: pr-info/

    - name: Save Firmwares Cache
      if: steps.cache-firmwares.outputs.cache-hit != 'true'
      uses: actions/cache/save@v4
      with:
        path: firmware/
        key: firmwares-${{ runner.os }}-${{ runner.arch }}-${{ env.FIRMWARE_VERSION }}

    - name: Save Kernel Cache
      if: steps.cache-kernel.outputs.cache-hit != 'true'
      uses: actions/cache/save@v4
      with:
        path: tmp/kernel/
        key: kernels-${{ runner.os }}-${{ runner.arch }}-${{ env.KERNEL_SHA }}

    - name: Save ccache
      uses: actions/cache/save@v4
      with:
        path: tmp/ccache/
        key: ccache-${{ runner.os }}-${{ runner.arch }}-${{ github.head_ref || github.ref_name }}

    - name: Save downloads cache
      uses: actions/cache/save@v4
      with:
        path: |
          tmp/cache/**/.git
          tmp/cache/**/*.zip
          tmp/cache/**/*.gz
          tmp/cache/**/*.xz
        key: downloads-${{ runner.os }}-${{ runner.arch }}-${{ github.head_ref || github.ref_name }}
